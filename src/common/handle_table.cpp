// src/common/handle_table.cpp
// HandlePool is a header-only template — this file exists for the CMake
// dxbridge_common target and can be used for explicit instantiations if needed.
#include "handle_table.hpp"

// No explicit instantiations needed since HandlePool is fully defined in the
// header and each translation unit that uses it will instantiate as needed.
